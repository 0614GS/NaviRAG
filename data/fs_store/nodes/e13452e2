{"node_id": "e13452e2", "title": "Definition", "path": "functional-api > Functional API overview > Task > Definition", "content": "Tasks are defined using the `@task` decorator, which wraps a regular Python function.\n\n```python  theme={null}\nfrom langgraph.func import task\n\n@task()\ndef slow_computation(input_value):\n    # Simulate a long-running operation\n    ...\n    return result\n```\n\n<Warning>\n  **Serialization**\n  The **outputs** of tasks must be JSON-serializable to support checkpointing.\n</Warning>", "summary": "Defines tasks using the @task decorator in LangGraph, requiring JSON-serializable outputs for checkpointing.", "keywords": ["@task", "decorator", "JSON-serializable", "checkpointing", "slow_computation", "task", "outputs", "serialization"]}