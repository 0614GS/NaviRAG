{"node_id": "3c4e0ac6", "title": "1. Install the LangGraph CLI", "path": "local-server > Run a local server > 1. Install the LangGraph CLI", "content": "<CodeGroup>\n  ```bash pip theme={null}\n  # Python >= 3.11 is required.\n  pip install -U \"langgraph-cli[inmem]\"\n  ```\n\n  ```bash uv theme={null}\n  # Python >= 3.11 is required.\n  uv add \"langgraph-cli[inmem]\"\n  ```\n</CodeGroup>", "summary": "Instructions for installing the LangGraph CLI using pip or uv, requiring Python 3.11 or higher.", "keywords": ["LangGraph CLI", "pip", "uv", "inmem", "Python 3.11", "install", "command-line interface"]}