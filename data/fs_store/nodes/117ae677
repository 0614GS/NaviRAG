{"node_id": "117ae677", "title": "Actors", "path": "pregel > LangGraph runtime > Actors", "content": "An **actor** is a `PregelNode`. It subscribes to channels, reads data from them, and writes data to them. It can be thought of as an **actor** in the Pregel algorithm. `PregelNodes` implement LangChain's Runnable interface.", "summary": "An actor is a PregelNode that subscribes to, reads from, and writes to channels, implementing LangChain's Runnable interface.", "keywords": ["actor", "PregelNode", "channels", "Pregel algorithm", "LangChain", "Runnable interface"]}