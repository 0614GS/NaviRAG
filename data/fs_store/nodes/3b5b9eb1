{"node_id": "3b5b9eb1", "title": "3. Install dependencies", "path": "local-server > Run a local server > 3. Install dependencies", "content": "In the root of your new LangGraph app, install the dependencies in `edit` mode so your local changes are used by the server:\n\n<CodeGroup>\n  ```bash pip theme={null}\n  cd path/to/your/app\n  pip install -e .\n  ```\n\n  ```bash uv theme={null}\n  cd path/to/your/app\n  uv sync\n  ```\n</CodeGroup>", "summary": "Instructions for installing dependencies in edit mode for a LangGraph app using pip or uv.", "keywords": ["LangGraph", "dependencies", "pip", "uv", "edit mode", "local server", "install", "sync", "root directory", "app"]}