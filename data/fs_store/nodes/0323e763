{"node_id": "0323e763", "title": "How it works", "path": "graph-api > Graph API overview > Runtime context > Accessing and handling the recursion counter > How it works", "content": "The step counter is stored in `config[\"metadata\"][\"langgraph_step\"]`. The recursion limit check follows the logic: `step > stop` where `stop = step + recursion_limit + 1`. When the limit is exceeded, LangGraph raises a `GraphRecursionError`.", "summary": "Explains the step counter storage and recursion limit check logic in LangGraph, including error handling for exceeded limits.", "keywords": ["step counter", "config", "metadata", "langgraph_step", "recursion limit", "stop", "GraphRecursionError", "LangGraph"]}